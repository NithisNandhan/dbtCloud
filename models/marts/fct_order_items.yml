models:
  - name: fct_order_items
    config:
      contract:
        enforced: true
    columns:
      - name: order_item_id
        data_type: varchar
        constraints:
          - type: not_null

      - name: order_id
        data_type: varchar

      - name: product_id
        data_type: varchar

      - name: location_id
        data_type: varchar

      - name: customer_id
        data_type: varchar

      - name: order_total
        data_type: decimal

      - name: tax_paid
        data_type: decimal

      - name: ordered_at
        data_type: timestamp

      - name: customer_name
        data_type: varchar

      - name: location_name
        data_type: varchar

      - name: tax_rate
        data_type: int

      - name: location_opened_at
        data_type: timestamp

      